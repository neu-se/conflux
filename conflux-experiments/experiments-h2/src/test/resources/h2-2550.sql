DROP TABLE IF EXISTS t;
CREATE TABLE t (a int, b int);

MERGE INTO t
USING (SELECT 1 a) s
ON (a = 1)
WHEN NOT MATCHED AND s.a = 1 THEN INSERT (a, b) VALUES (1, 1)
WHEN NOT MATCHED AND s.b = 2 THEN INSERT (a, b) VALUES (2, 2);